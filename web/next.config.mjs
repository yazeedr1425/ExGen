/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The generated extension code is user data, never executed here. It is only
  // ever rendered as text inside <pre>, so no CSP relaxation is needed.
  poweredByHeader: false,
};

export default nextConfig;
